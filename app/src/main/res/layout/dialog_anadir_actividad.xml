<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="480dp"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_dialog">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:paddingTop="28dp"
        android:paddingBottom="28dp">

        <TextView
            android:id="@+id/tvTituloDialogActividad"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="AÑADIR ACTIVIDAD"
            android:textSize="@dimen/text_size_large"
            android:textStyle="bold"
            android:textColor="#1C1C1E"
            android:gravity="center"
            android:layout_marginBottom="16dp"/>

        <!-- BARRA DE PROGRESO -->
        <LinearLayout
            android:id="@+id/dotContainerAct"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="16dp">
            <View android:id="@+id/dotStep1" android:layout_width="16dp" android:layout_height="16dp" android:background="@drawable/step_dot_active" android:layout_marginEnd="8dp"/>
            <View android:id="@+id/dotStep2" android:layout_width="16dp" android:layout_height="16dp" android:background="@drawable/step_dot_inactive" android:layout_marginEnd="8dp"/>
            <View android:id="@+id/dotStep3" android:layout_width="16dp" android:layout_height="16dp" android:background="@drawable/step_dot_inactive" android:layout_marginEnd="8dp"/>
            <View android:id="@+id/dotStep4" android:layout_width="16dp" android:layout_height="16dp" android:background="@drawable/step_dot_inactive"/>
        </LinearLayout>

        <!-- PASOS DEL WIZARD (INCLUIDOS) -->
        <include layout="@layout/wizard_step_0_act" />
        <include layout="@layout/wizard_step_act_tipo" />
        <include layout="@layout/wizard_step_act_desc" />
        <include layout="@layout/wizard_step_act_hora" />
        <include layout="@layout/wizard_step_act_dias" />
        <include layout="@layout/wizard_step_act_summary" />
        <include layout="@layout/wizard_step_act_edit_choice" />

        <!-- FOOTER (MIC + BOTONES) -->
        <include layout="@layout/wizard_footer_act" />

    </LinearLayout>

    <!-- Botón X de cierre -->
    <TextView
        android:id="@+id/btnCancelarDialogActividad2"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        android:text="✕"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#1C1C1E"
        android:gravity="center"
        android:background="@drawable/bg_tipo_normal"
        android:clickable="true"
        android:focusable="true"/>

</FrameLayout>